# Verification and recovery

If a render fails, preserve the error and the smallest reproducible project state in scratch. Check missing media, unsupported codecs, font substitution, frame rate, color space, and audio duration before changing the edit. Re-render a short segment after each fix; do not spend a full render on a guess.
